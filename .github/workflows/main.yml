name: CD
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy to production
      uses: appleboy/ssh-action@master
      with:
        username: woatdqdp
        host: 162.0.232.119
        password: ${{ secrets.SSH_PASSWORD }}
        script: 'cd /application && ./server_deploy.sh'
